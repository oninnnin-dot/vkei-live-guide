---
interface Props {
  title: string;
  text: string;
}

const { title, text } = Astro.props;
---

<div class="gothic-card rounded-xl p-5">
  <p class="font-classical pt-2 font-black text-mist">{title}</p>
  <p class="mt-2 text-sm leading-7 text-muted">{text}</p>
</div>
