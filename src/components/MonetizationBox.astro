---
interface Item {
  title: string;
  text: string;
}

interface Props {
  title?: string;
  lead?: string;
  items?: Item[];
}

const {
  title = '遠征前に用意しておくと安心なもの',
  lead = '物販・雨・終演後移動がある日は、荷物を減らしつつ必要なものだけ持つのがおすすめです。',
  items = [
    { title: '持ち物おすすめ枠', text: 'チケット、身分証、現金、IC、モバイルバッテリー、雨具を小さくまとめる前提で準備します。' },
    { title: '遠征便利グッズ枠', text: '圧縮袋、折りたためるサブバッグ、薄手の上着があると、物販後や夜行移動で動きやすくなります。' },
    { title: 'ホテル・宿泊エリア案内枠', text: '終演後に焦らないよう、駅までの距離とチェックイン時間を先に見ておくと安心です。' },
    { title: '雨の日対策グッズ枠', text: '折りたたみ傘、防水袋、替えの靴下、タオルを増やしすぎない範囲で用意します。' },
    { title: 'モバイルバッテリー枠', text: '電子チケット、地図、終電確認を使う日は、軽量タイプを1つ入れておくと余裕が残ります。' },
  ],
} = Astro.props;
---

<aside class="gothic-card rounded-xl p-5 md:p-6">
  <p class="gothic-label">Preparation</p>
  <h2 class="font-classical mt-2 text-2xl font-black leading-tight text-mist">{title}</h2>
  <p class="mt-3 text-sm leading-7 text-muted">{lead}</p>
  <div class="mt-5 grid gap-3 md:grid-cols-2">
    {items.map((item) => (
      <div class="rounded-md border border-amber/20 bg-black/25 p-4">
        <p class="font-classical font-black text-mist">{item.title}</p>
        <p class="mt-2 text-sm leading-7 text-muted">{item.text}</p>
      </div>
    ))}
  </div>
  <p class="mt-4 text-xs leading-6 text-muted">
    この枠は今後の広告リンク設置を見据えた案内枠です。現時点では商品購入を前提にせず、当日の動きやすさを優先して整理しています。
  </p>
</aside>
